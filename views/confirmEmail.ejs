<!DOCTYPE html>
<html lang="pt-br">
  <%- include('partials/head', { title: 'Confirme seu Cadastro!' }) %>
  <body>
    <%- include('partials/header') %>

    <div class="container text-center mt-5">
      <div class="thank-you-content">
        <h1 class="mt-3 text-success">Confirme seu Cadastro</h1>
        <p class="lead">Obrigado por se juntar ao <strong>ChamaChurras</strong>, <%= user.name %>!</p>
        <p>Para concluir, insira o código de confirmação que enviamos para seu e-mail.</p>
    
        <% if (error) { %>
          <div class="alert alert-danger mt-4" role="alert">
            <%= error %>
          </div>
        <% } %>
    
        <form action="/auth/confirm-email" method="POST" class="mt-4">
          <input type="hidden" name="email" value="<%= user.email %>">
          <div class="form-group">
              <label for="code">Código de Confirmação</label>
              <input type="text" class="form-control text-center" id="code" name="code" required placeholder="Digite o código">
          </div>
          <button type="submit" class="btn btn-success mt-3">Confirmar E-mail</button>
        </form>
      </div>
    </div>

    <%- include('partials/footer') %>
  </body>
</html>