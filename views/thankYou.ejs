<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agradecimento - ChamaChurras</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            color: #343a40;
            font-family: 'Arial', sans-serif;
        }
    
        .thank-you-container {
        text-align: center;
        margin-top: 1%; /* Reduzido de 10% para 5% */
        padding: 20px;
    }
    
        .thank-you-title {
            font-size: 2.5rem;
            color: #28a745;
            font-weight: bold;
        }
    
        .thank-you-message {
            font-size: 1.2rem;
            margin-top: 20px;
            margin-bottom: 20px;
        }
    
        .review-section {
            margin-top: 30px;
            text-align: left;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 15px;
            background-color: #fff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
    
        .review-section h2 {
            color: #28a745;
            font-size: 1.5rem;
            margin-bottom: 15px;
            text-align: center;
        }
    
        .review-section table {
            width: 100%;
            margin: 0 auto;
        }
    
        .review-section td {
            padding: 8px 5px;
            vertical-align: top;
        }
    
        .review-section tr:nth-child(even) {
            background-color: #f9f9f9;
        }
    
        .whatsapp-button {
            margin-top: 30px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }
    
        .whatsapp-button img {
            width: 30px;
            margin-right: 10px;
        }
    
        .brand-logo {
            width: 150px; /* Aumenta o tamanho da logo */
            margin-top: 2px;
        }
    
        .whatsapp-button a {
            text-decoration: none;
            color: white;
        }
    </style>
</head>
<body>
    <div class="thank-you-container">
        <img src="/images/logo.png" alt="Logo ChamaChurras" class="brand-logo">   
        <h1 class="thank-you-title">Obrigado pelo seu pedido!</h1>
        <p class="thank-you-message">
            Nós, do <strong>ChamaChurras</strong>, estamos muito felizes em ajudar a tornar seu churras especial.
        </p>
        
        <!-- Review Section -->
<div class="review-section">
    <h2 class="text-center">Informações do Pedido</h2>
    <table class="table table-borderless">
        <tbody>
            <tr>
                <td><strong>Nome:</strong></td>
                <td><%= formData.name %></td>
            </tr>
            <tr>
                <td><strong>Email:</strong></td>
                <td><%= formData.email %></td>
            </tr>
            <tr>
                <td><strong>WhatsApp:</strong></td>
                <td><%= formData.phone %></td>
            </tr>
            <tr>
                <td><strong>Data do Evento:</strong></td>
                <td><%= formData.eventDate %></td>
            </tr>
            <tr>
                <td><strong>Local do Evento:</strong></td>
                <td><%= formData.eventLocation %></td>
            </tr>
            <tr>
                <td><strong>Número de Convidados:</strong></td>
                <td><%= formData.numberOfGuests %></td>
            </tr>
            <tr>
                <td><strong>Tipo de Evento:</strong></td>
                <td><%= formData.eventType %></td>
            </tr>
            <tr>
                <td><strong>Solicitações Especiais:</strong></td>
                <td><%= formData.specialRequests %></td>
            </tr>
        </tbody>
    </table>
</div>

        <p class="thank-you-message">
            Agora entre em contato com o administrador do  <strong>ChamaChurras</strong> para seguir com a confirmação pelo seu <strong>WhatsApp!</strong>
        </p>

        <!-- Botão de WhatsApp -->
        <div class="whatsapp-button">
            <img src="/images/whatsapp-logo.png" alt="WhatsApp Logo">
            <a id="whatsapp-link" class="btn btn-success btn-lg" target="_blank">
                <i class="fab fa-whatsapp"></i> Entrar em contato pelo WhatsApp
            </a>
        </div>
    </div>

    <script>
        // Função para gerar a mensagem com os dados do formulário
        function generateWhatsAppMessage() {
            // Capturar os dados diretamente do back-end ou da renderização
            const userInfo = {
                name: "<%= formData.name %>",
                email: "<%= formData.email %>",
                phone: "<%= formData.phone %>",
                eventDate: "<%= formData.eventDate %>",
                eventLocation: "<%= formData.eventLocation %>",
                guests: "<%= formData.numberOfGuests %>",
                eventType: "<%= formData.eventType %>",
                specialRequests: "<%= formData.specialRequests %>"
            };
    
            // Mensagem formatada
            const message = `
    Olá, fiz uma solicitação pelo ChamaChurras!
    - Nome: ${userInfo.name}
    - Email: ${userInfo.email}
    - WhatsApp: ${userInfo.phone}
    - Data do Evento: ${userInfo.eventDate}
    - Local do Evento: ${userInfo.eventLocation}
    - Número de Convidados: ${userInfo.guests}
    - Tipo de Evento: ${userInfo.eventType}
    - Solicitações Especiais: ${userInfo.specialRequests}
    
    Aguardo o retorno!`.trim();
    
            // Codificar a mensagem para uso na URL
            const encodedMessage = encodeURIComponent(message);
    
            // Atualizar o link para redirecionar ao WhatsApp
            const whatsappLink = `https://wa.me/5541985028480?text=${encodedMessage}`;
            document.getElementById("whatsapp-link").href = whatsappLink;
        }
    
        // Chamar a função ao carregar a página
        document.addEventListener("DOMContentLoaded", generateWhatsAppMessage);
    </script>
    
</body>
</html>